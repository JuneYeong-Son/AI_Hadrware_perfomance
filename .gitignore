# Dependencies
node_modules/

# Environment
.env
.env.*

# Build output
dist/
build/

# Logs
*.log
!application/reports/
!application/reports/*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Locally generated GPU baselines (per-machine data, not source)
application/baselines/
# Cumulative usage/throttle observations (per-machine data)
application/monitor/
# Generated validation reports (keep example .log only)
application/reports/*.txt

# Secrets / credentials — never commit
application/kaggle.json
kaggle.json

# Local datasets (not source)
application/data/

# Local per-deployment app config (points at your server; not source).
# The committed template is packaging/gpuperf.config.example.json
/gpuperf.config.json

# Windows installer output
installer_out/
# PyInstaller generated spec (builds use CLI flags; spec is disposable)
*.spec
